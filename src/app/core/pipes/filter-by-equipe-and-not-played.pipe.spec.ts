import { FilterByEquipeAndNotPlayedPipe } from './filter-by-equipe-and-not-played.pipe';

describe('FilterByEquipeAndNotPlayedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByEquipeAndNotPlayedPipe();
    expect(pipe).toBeTruthy();
  });
});
