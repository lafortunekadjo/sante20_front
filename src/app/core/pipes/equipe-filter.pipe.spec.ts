import { EquipeFilterPipe } from './equipe-filter.pipe';

describe('EquipeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
