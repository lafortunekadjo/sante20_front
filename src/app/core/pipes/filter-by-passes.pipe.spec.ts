import { FilterByPassesPipe } from './filter-by-passes.pipe';

describe('FilterByPassesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPassesPipe();
    expect(pipe).toBeTruthy();
  });
});
