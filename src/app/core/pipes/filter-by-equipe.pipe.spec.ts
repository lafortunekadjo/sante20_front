import { FilterByEquipePipe } from './filter-by-equipe.pipe';

describe('FilterByEquipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByEquipePipe();
    expect(pipe).toBeTruthy();
  });
});
