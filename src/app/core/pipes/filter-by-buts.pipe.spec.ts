import { FilterByButsPipe } from './filter-by-buts.pipe';

describe('FilterByButsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByButsPipe();
    expect(pipe).toBeTruthy();
  });
});
