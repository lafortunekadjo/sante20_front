import { CurrencyXafPipe } from './currency-xaf.pipe';

describe('CurrencyXafPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyXafPipe();
    expect(pipe).toBeTruthy();
  });
});
